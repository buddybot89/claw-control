# Backend Configuration
# =====================

# Database Connection
# Use SQLite (easy local setup):
DATABASE_URL=sqlite:./data/claw-control.db

# Or PostgreSQL (Railway/production):
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server Port
PORT=3001

# API Key (optional - for protected endpoints)
# Generate: openssl rand -hex 32
API_KEY=
