# Database Configuration
# ======================

# Use SQLite (easy local setup - no external database needed):
DATABASE_URL=sqlite:./data/claw-control.db

# Or PostgreSQL (production):
# DATABASE_URL=postgresql://claw:clawcontrol@localhost:5432/claw_control

# PostgreSQL Configuration (only needed if using Postgres)
POSTGRES_USER=claw
POSTGRES_PASSWORD=clawcontrol
POSTGRES_DB=claw_control

# Backend Configuration
PORT=3001

# Authentication (Optional)
# =========================
# Set API key for protected endpoints (POST/PUT/DELETE operations)
# Leave empty for open access (recommended for local development)
# Generate a secure key for production: openssl rand -hex 32
API_KEY=

# Frontend Configuration
VITE_API_URL=http://localhost:3001
